'use client'

import { useEffect, useState } from 'react'

const lines = ["About", "Me"]

interface HomeLine {
  text: string
  delay: number
  className: string
}

function TypeWriterEffect({ text, delay, className }: HomeLine) {
    useEffect(() => {
    
  }, [])

  return(
    <div>
        
    </div>
  )
}